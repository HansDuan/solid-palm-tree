# ClassPilot（智课领航员）· 对接契约与 9/26 提交清单

> 用途：B 同学（AI 批改内核 + 演示服务）留给 **A（前端）** 与 **C（后端/素材/部署）** 的对接契约，以及 9/26 比赛提交清单。
> 最后更新：2026-09-24。内核代码位于 `classpilot/`，零依赖演示服务 `node demo/server.mjs` 已可独立运行。

---

## 0. ⚠️ 当前最高优先级阻塞（务必先读）

**`hy3`（混元文本批改模型）免费试用额度已耗尽，所有真实批改接口当前返回 HTTP 402。**

- 现象：`POST /api/grade/async`、`/api/dashboard`、`/api/agent` 均返回
  `{ "ok": false, "error": "…402… The free trial quota for the service has been exhausted and postpaid billing is not enabled …" }`
- 已验证：同账号下 **`hy-vision-2.0-instruct`（视觉模型）仍有额度可用**（200）；其它模型名（hunyuan-turbos / hunyuan-lite / hunyuan-pro 等）在本 TokenHub 网关**不存在**（400）。
- 结论：**本环境唯一合规且存在文本模型就是 `hy3`，没有可替换的「其它文本模型」**。因此真实批改必须先恢复 `hy3` 额度。
- **需要账号持有者（协调人/你）操作**：到 TokenHub（tokenhub.tencentmaas.com）对应 `hy3` 服务控制台 → 开通**后付费（按量计费）**并绑定支付方式。开通后无需改代码，接口立即恢复 REAL 批改。
- 影响范围：
  - Task #18（Agent 四工具真实联调）⏸ 暂停，待额度恢复后 10 分钟内可补完。
  - Task #19（v4 prompt 精简降 MAE + 重跑真实评估）⏸ 暂停，待额度恢复后重跑。
  - **不影响**以下已完成的真实交付：规则类接口（`/api/grade/choice-batch`、`/api/plagiarism` 词袋查重）不调模型，永远返回 REAL 数据；所有接口 JSON 契约已稳定。

> 合规红线（再强调）：所有对外文档/演示只允许出现 `hy3` + `hy-vision-2.0-instruct`（且仅腾讯混元）；指标必须真实，v4 实测 MAE 9.3 已如实记录，**绝不允许用 MOCK 冒充 REAL 评估数据**。

### 0.1 已实测排除的两条「替代模型」捷径（千万别再走）

为绕开 hy3 的 402，本会话已实测两条看似可行的替代路径，**结论都是不可用**，请勿再花时间尝试：

| 候选模型 | 网关实测 | 合规？ | 能否产出批改 JSON | 结论 |
|----------|----------|--------|------------------|------|
| `kimi-k3`（月之暗面 / Moonshot） | `/v1/messages` 返回 200 | ❌ **非腾讯混元** | 能 | **严禁进入产品/演示/视频**——违反"仅腾讯混元"红线；仅作"网络与网关通"的诊断信号，不可当真 |
| `hy4-preview`（腾讯混元推理模型） | 简单「你好」返回 200；批改 JSON prompt 返回 `stop_reason:"max_tokens"`、`output_tokens:4096` | ✅ 合规 | ❌ 不能——全部输出 token 烧在内部 `thinking` 块，**content 仅有 `thinking`，无 `text`/JSON** | 与 9/22 hy4-preview 超时同源；无法解析，不可用于真实批改 |

**关键认知（已验证）：**
- hy3 的 402 是 **`401008` 免费试用额度耗尽 + 未开后付费**，与模型名 / 端点 / Key 是否有效**无关**——**所有**被测 Key（含两个新 Key 与旧 `.env.local` Key）在两个端点（`/v1/messages` 与 `/v1/chat/completions`）**全部 402**。100% 是额度问题。
- 因此**唯一合规且可行的真实批改解锁方式 = 账号持有者开通 hy3 后付费**。不存在模型侧 workaround，也不要再试其它模型名。

---

## 1. 9/26 提交清单（对照赛制要求）

| # | 提交物 | 状态 | 负责人 | 说明 |
|---|--------|------|--------|------|
| 1 | **线上可访问链接** | 🟡 临时方案就绪 / 需 C 云端化 | B（临时）+ C（正式） | 临时入口：`https://fd68cb5e5ca84fe29cd17d884628a60a.app.workbuddy.host`（CloudStudio 静态页，sandboxId 固定→链接永久）。页内"打开实时演示"指向免费隧道，**本机开机才有效**，仅作评委预览兜底。 |
| 2 | **3 分钟演示视频** | ⚪ 待录 | 全员 | 脚本见 `演示视频脚本.md`：登录→上传作业照片(OCR)→实时异步批改(轮询进度)→学情看板→预警名单→薄弱生练习。⚠️ 录制前务必先恢复 hy3 额度，否则视频里批改会 402。 |
| 3 | **PPT / 路演稿** | ⚪ 待补 | A + 协调人 | 架构图、合规说明（仅混元）、真实指标（MAE 9.3、解析成功率 96.7%、30 人班级并发 6 路）。 |
| 4 | **源码仓库** | 🟢 本地 Git 已就绪 | B | `classpilot/` 已 `git init`，`.gitignore` 已屏蔽 `.env.local` / `.run/`。远程推送需协调人账号（gh 未装，见 §5）。 |
| 5 | **LearnBuddy 使用日志** | 🟢 本会话即证据 | B | 本对话全程为 LearnBuddy 协同开发记录，可作为"使用 AI 编程工具"佐证。 |

---

## 2. A 同学对接契约（前端 ↔ 演示/内核服务）

### 2.1 运行与代理
- 演示服务：`node demo/server.mjs` → `http://127.0.0.1:3111`
- **前端（Vue，端口 5173）必须设置代理目标**：
  ```
  VITE_PROXY_TARGET=http://localhost:3111
  ```
  或 vite.config 中 `server.proxy['/api'] = 'http://127.0.0.1:3111'`。
- 前端页面 ↔ 后端接口映射建议：

| 前端页面 | 调用接口 |
|----------|----------|
| 登录 / 首页 | `GET /api/health`、`GET /api/mode` |
| 作业管理（列表/新建） | `GET /api/assignments`、`POST /api/assignments` |
| 批改工作台（单份/拍照） | `POST /api/grade/async` → 轮询 `GET /api/grade/async?id=`；拍照走 `POST /api/upload` 或先 `POST /api/ocr` 再 `POST /api/grade/async` |
| 选择题批量 | `POST /api/grade/choice-batch`（纯规则，无需模型，可随时演示） |
| 学情看板 | `GET /api/dashboard?assignmentId=A1` |
| 查重 | `GET /api/plagiarism?assignmentId=A1&threshold=0.5` |
| 班级周报 | `POST /api/report/weekly`（body `{ "assignmentId":"A1" }`） |
| 个性化练习 | `POST /api/exercises`（body `{ "weakPoints":["函数","循环"] }`） |
| 智能助手（4 意图） | `POST /api/agent`（body `{ "message": "..." }`） |
| 教师反馈 | `POST /api/feedback` |

### 2.2 接口契约（路径 / 方法 / 入参 / 返回，均为 JSON）

**GET `/api/health`** → `{ ok, mode:{baseUrl,textModel,visionModel,...}, memory, maxPages, storage }`

**GET `/api/mode`** → `{ mode:"REAL"|"MOCK", storage }`

**GET `/api/assignments`** → `{ ok:true, assignments:[ {id,course,title,type,description,answerKey?} ] }`
（当前内置 A1–A7：code / math / choice / fill / calc / english 六种题型）

**POST `/api/assignments`** body `{id?,course,title,type,description,answerKey?}` → `{ ok:true, assignment }`；重复 id 返回 400。

**POST `/api/grade/async`** body `{ text, assignmentId? }` → 立即 `{ ok:true, jobId }`（**不等模型**）
**GET `/api/grade/async?id=`** →
- 进行中：`{ ok:true, id, status:"running", elapsedMs }`
- 完成：`{ ok:true, id, status:"done", elapsedMs, assignment, result:{ total_score, dimensions:[{name,score,max,confidence}], summary, suggestion } }`
- 失败：`{ ok:true, id, status:"error", elapsedMs, error:"…402…" }` ← **前端需渲染错误态，不要假设一定有 result**
**建议前端轮询间隔 2.5s，最长 150s。**

**POST `/api/grade/choice-batch`** body `{ key, submissionsText, totalPoints? }`（key 如 `1.B 2.C 3.A 4.B`；submissionsText 每行一名学生，支持 `张三: B C A B` 或 `B C A B`）→
`{ ok:true, key, nQuestions, perQuestionPoints, totalPoints, count, avg, highest, lowest, passRate, questionStats:[{q,expected,correctRate,wrongCount}], results:[{name,score,correct,total,ratio,perQuestion}] }`
（**纯规则、永远 REAL，最适合评委现场点测**）

**GET `/api/dashboard?assignmentId=`** → 成功 `{ ok:true, assignmentId, avg, highest, lowest, distribution:[{range,count}], dimensions:[{name,avg,max,rate}], lowConfidence, errored, erroredNames }`；当模型不可用时返回 `{ ok:false, error }`（前端渲染"批改服务暂不可用，请检查额度"）。

**GET `/api/plagiarism?assignmentId=&threshold=0.5`** → `{ ok:true, assignmentId, threshold, checkedCount, flagged, pairs:[{a,b,similarity}] }`。采用 **C 的 TF-IDF + 余弦查重算法**（`lib/analytics.js` 纯函数，无模型依赖，永远 REAL）；`checkedCount` 为参与比对的提交数（按 `student_id` 去重，避免"自己抄自己"误报）。默认阈值 0.5（演示更干净），调低到 0.3 可见更多疑似对（规划书默认）。演示库已植入 1 组 3 人抄袭簇（相似度 1.0）用于展示命中。

**POST `/api/report/weekly`** body `{ assignmentId }` → 成功 `{ ok:true, summary, polished:boolean, stats:{submissionCount,gradedCount,dimensionAverages} }`；无批改数据时 `{ ok:true, summary:"暂无批改数据，无法生成周报。", stats }`。周报数字**仅来自 REAL 批改结果**（`grading_results` 的 `mode='REAL'`），绝不混入 MOCK；有 Key 时由混元润色（`polished:true`），无 Key / 模型异常自动降级本地模板（`polished:false`，演示不断流）。

**POST `/api/exercises`** body `{ weakPoints:["函数","循环"] }` → `{ ok:true, items:[{ knowledgePoint, question, polished:boolean }] }`。有 Key 时由混元出题，无 Key / 异常降级模板占位。

**POST `/api/agent`** body `{ message }` → 成功 `{ ok:true, tool, data, reply }`；模型不可用时 `{ ok:false, error }`。四意图：`grade_assignment` / `get_class_report` / `generate_warning_list` / `generate_practice`。

**POST `/api/upload`**（`multipart` 或 JSON `{ files:[{filename,contentBase64}] }`）→ OCR+批改一体；返回 `{ ok, submissionId, resultId, source, transcript, charCount, result }`。照片正上方俯拍、光线均匀、字迹占画面 2/3 以上识别率最高。

**POST `/api/ocr`** 仅做多图转文字（不批改）→ `{ ok, source, transcript, text, pages, warnings }`。

**POST `/api/answerkey`** body `{ assignmentId?, text? , files? }` 教师上传/更新标准答案键。

> 注：同步 `POST /api/grade`（单份同步）仍保留但**不推荐**——受隧道 ~120s 上限与浏览器超时影响，正式联调一律用 `/api/grade/async`。

---

## 3. C 同学对接契约（后端/素材/部署 ↔ B 内核）

### 3.1 B 已交付、C 可直接复用的内核入口
- 批改内核：`lib/grading/pipeline.js` 的 `gradeAssignment({assignment, studentText})` → 统一返回 `{ total_score, dimensions, summary, suggestion }`。
- 四 Agent 工具在 `lib/store/memory.js` 的 `TOOLS` 表，函数：`tool_grade_assignment` / `tool_get_class_report` / `tool_generate_warning_list` / `tool_generate_practice`。**C 接真实 SQLite/云库时只需替换这些函数的实现，接口（入参/返回字段）保持不变**，前端无需改动。
- 规则类：`lib/grading/choice-batch.js` 的 `gradeChoiceBatch`（选择题纯规则，无模型依赖）。

### 3.2 C 必须修复的 3 条合规红线（影响评审扣分）
1. **模型名合规**：架构图/文档/代码里出现的 `DeepSeek` / `GLM` / `hunyuan-turbos` 等**一律改为仅 `hy3`（文本）+ `hy-vision-2.0-instruct`（视觉）**，否则违反"仅腾讯混元"要求。
2. **FastAPI 自相矛盾**：设计文档里出现的技术栈表述（如自称 FastAPI 但又描述不符）需与真实后端栈对齐，避免评委追问时前后不一致。
3. **过期 `HUNYUAN_BASE_URL` / 鉴权头**：默认端点已统一为 `https://tokenhub.tencentmaas.com/v1`（C 同学 9/24 更新确认旧 `api.hunyuan.cloud.tencent.com` 已下线），鉴权头用 `x-api-key`（非老 `Bearer`）。B 的 `lib/grading/llm.js` 已按 URL 自动选鉴权头——若后续拿到可用的腾讯云官方混元 Key，只需改 `.env.local` 的 `HUNYUAN_BASE_URL` 与 `HUNYUAN_TEXT_MODEL`（如 hunyuan-standard），仍属"仅腾讯混元"合规范围，无需改业务代码。

### 3.3 C 的部署死线（9/26）
- 临时演示依赖本机开机 + 免费隧道，**正式提交必须走云端部署**（独立后端服务 + 公网域名），否则评委无法稳定访问。
- C 部署后请把**生产域名**回填到 `showcase/index.html` 的"打开实时演示"按钮，并 CloudStudio 重新部署（sandboxId 不变，链接永久）。

---

## 4. B 已完成工作清单（交接用）

- ✅ 零依赖演示服务 `demo/server.mjs`（端口 3111），含 16+ 路由。
- ✅ 异步批改（`/api/grade/async` + 轮询），解决隧道/浏览器超时。
- ✅ 选择题批量批改（纯规则，REAL，评委可现场点测）。
- ✅ 学情看板 `/api/dashboard`（30 人班级 6 路并发，单份失败不影响全班）。
- ✅ 查重接口 `/api/plagiarism`（**C 的 TF-IDF + 余弦算法已并入 `lib/analytics.js`**，纯函数无模型依赖，永远 REAL；按 `student_id` 去重避免自配误报；默认阈值 0.5，演示库含 1 组 3 人抄袭簇用于命中展示）。
- ✅ 班级周报 `POST /api/report/weekly`（数字仅来自 REAL 批改 `mode='REAL'`，绝不混入 MOCK；有 Key 由混元润色 `polished:true`，无 Key / 异常自动降级本地模板 `polished:false`，演示不断流）。
- ✅ 个性化练习 `POST /api/exercises`（薄弱知识点出题，有 Key 由混元生成，无 Key 降级模板占位 `polished:false`）。
- ✅ 作业 CRUD `/api/assignments`（A 的作业管理页）。
- ✅ Agent 四工具调度（`/api/agent`）。
- ✅ OCR 上传 `/api/upload`、`/api/ocr`（拍照→文字→批改）。
- ✅ 公网隧道守护 `tools/tunnel.mjs` + CloudStudio 固定入口 + 每小时看门狗 automation。
- ✅ 真实评估：v4 prompt 实测 **MAE 9.3、解析成功率 96.7%**（额度耗尽前跑出，已如实记录）。
- ✅ 3 分钟演示视频脚本 `演示视频脚本.md`（含录制前检查清单、3:00 时间轴、降级方案、镜头提示；⚠️ 录制前需先恢复 hy3 后付费额度）。

---

## 5. 本地 Git 与远程推送

- 本地仓库已就绪，`git init -b main`，`.gitignore` 屏蔽 `.env.local`、`.run/`（含 ssh 密钥与隧道地址、模型实测请求体）。
- **当前提交**：`4f43177`（初始提交，含 87 个文件，B-kernel 全部代码 + 演示服务 + 内核 + 30 人种子数据）。
- **远程**：`origin = https://github.com/HansDuan/solid-palm-tree.git`（A 同学建的仓库）。`.env.local` 已确认被忽略，不会进 Git。
- **⏸ 推送状态：未完成（两重阻塞）**
  1. 本沙箱当前**连不通 github.com**（curl / git 443 超时），疑似临时网络中断；
  2. 推送需要 **A 同学的 GitHub PAT**（本环境无 GITHUB_TOKEN / GH_TOKEN）。
- 一旦网络恢复 + 拿到 PAT，执行（A 的仓库初始只有一个 README，需允许无关联历史合并）：
  ```bash
  cd classpilot
  git fetch origin
  git merge origin/main --allow-unrelated-histories -X ours --no-edit   # 保留我们全部内容，并入 A 的 README
  git push -u origin main
  ```
- ⚠️ 教训（已记入 memory）：对共享仓库做 `merge --allow-unrelated-histories` 时**不要开 autostash**，且先备份 `.git`；本会话曾因 autostash 失败导致 `.git` 短暂丢失，已用 `git init` 重建为单提交 `4f43177`（原 5 段历史未恢复，但代码与密钥屏蔽均完好）。

---

## 6. 今晚/明早行动清单（按优先级）

1. 🔴 **恢复 hy3 后付费额度**（协调人操作 TokenHub 控制台）—— 解锁 Task #18/#19 与演示视频录制。
2. 🟢 A 按 §2 接前端（代理指向 3111，先联调 choice-batch / plagiarism 两个永远 REAL 的接口热身）。
3. 🟢 C 修 §3.2 三条红线 + 云端部署，回填生产域名到 showcase。
4. 🟡 额度恢复后：B 补完 Agent 四工具真实联调（Task #18）、重跑 v4 评估（Task #19）并刷新本文档指标。
5. 🟡 录制 3 分钟演示视频（务必在 ① 之后）。
