# ClassPilot（智课领航员）· 3 分钟演示视频脚本

> 目标：向评委展示「AI 作业批改 → 学情分析 → 查重预警 → 周报/练习」完整闭环。
> 合规底线：全程仅用腾讯混元（`hy3` 文本批改 + `hy-vision-2.0-instruct` 视觉），所有指标真实，**严禁用 MOCK 冒充 REAL**。

## 录制前必做（否则会穿帮）

1. ⚠️ **hy3 真实批改已解锁（当前未解锁，等 C 的 TokenHub key）**：模型接线已 100% 合规（hy3 文本 + hy-vision-2.0-instruct 视觉 / TokenHub 网关），但当前 key 仍 402/401008（免费额度耗尽、未开后付费）。解锁步骤：C 在 **TokenHub 控制台**（`console.cloud.tencent.com/tokenhub/inference`，勿用已下线的旧混元平台）确认额度并把该账号 key 发 B → 填 `.env.local` → 复测 `POST /api/grade/async` 返回 REAL（详见《对接契约与提交清单.md》§0.2）。未解锁前 `/api/grade/*`、`/api/dashboard`、`/api/agent`、周报润色、练习出题均 402。
2. ✅ 演示服务已启动：`node demo/server.mjs`（监听 `:3111`）。
3. ✅ 前端（Vue `:5173`）`VITE_PROXY_TARGET=http://localhost:3111` 已指向演示服务；`VITE_USE_MOCK=false`（必须走真实接口，否则违反「数据不造假」）。
4. ✅ 数据库已种子化：30 名学生真实提交（含 1 组 3 人抄袭簇，用于查重演示）。

## 时间轴（总时长 ≈ 3:00）

### 0:00–0:20 开场 + 登录（20s）
- 画面：打开浏览器进入系统首页 / 登录页。
- 操作：以 `teacher@demo / 123456` 登录（前端本地校验，无需后端）。
- 旁白：「智课领航员，面向编程与理科作业的 AI 批改与学情分析平台。下面用一次真实作业批改，演示从提交到学情闭环的全过程。」

### 0:20–0:55 拍照上传 + OCR + 异步批改（35s）
- 画面：进入「批改工作台」，点击上传一张**手写作答照片**（俯拍、字迹占画面 2/3、光线均匀）。
- 操作：上传 → 后端 OCR 转文字（`/api/upload` 或 `/api/ocr`）→ 点「开始批改」→ 触发 `POST /api/grade/async` 立即返回 jobId → **前端每 2.5s 轮询** `GET /api/grade/async?id=`。

- 旁白：「拍照即批改。OCR 把手写稿转为文本，异步大模型批改避免超时，前端轮询实时刷新——单份 hy3 批改约 45–104 秒。」
- 完成态展示：总分、各维度得分（正确性 / 复杂度 / 规范，含置信度）、评语与提升建议。

### 0:55–1:30 学情看板（35s）
- 画面：切到「学情看板」`GET /api/dashboard?assignmentId=A1`。
- 展示：平均分、最高 / 最低分、分数段分布、各维度均值雷达、低置信度 / 异常名单。
- 旁白：「30 人班级、6 路并发批改，单份失败不影响全班。看板把维度均值与异常生一目了然，老师一眼锁定薄弱点。」

### 1:30–2:00 预警名单 + 查重（30s）
- 画面：点开「预警名单」（低置信度 / 异常生）。
- 操作：切到「查重」`GET /api/plagiarism?assignmentId=A1&threshold=0.5`。
- 展示：命中 3 人抄袭簇（周雨萱 / 吴泽楷 / 郑清和，相似度 1.0），`checkedCount:30, flagged:3`。
- 旁白：「查重用 TF-IDF + 余弦，纯算法、无模型依赖、永远真实。这次作业发现一组三人雷同，直接标红预警。」

### 2:00–2:35 班级周报 + 个性化练习（35s）
- 画面：生成「班级周报」`POST /api/report/weekly` → 展示混元润色后的总结（数字严格来自 REAL 批改，绝不以模板冒充）。
- 操作：对薄弱知识点生成「个性化练习」`POST /api/exercises`（如「函数」「循环」）→ 展示题目 + 答案。
- 旁白：「周报数字全部来自真实批改结果；针对薄弱点，一键生成个性化练习题，形成教学闭环。」

### 2:35–3:00 合规与技术总结（25s）
- 画面：回到首页 / 架构卡片，定格。
- 旁白：「全程仅用腾讯混元（hy3 文本 + hy-vision 视觉），批改指标真实可查（v4 prompt 实测 MAE 9.3、JSON 解析率 96.7%）。智课领航员——让 AI 真正帮老师减负。」

## 容错预案（录制当天若某接口抖动）

- **永远可点测**：选择题批量 `POST /api/grade/choice-batch`（纯规则、秒级、无模型）与查重 `GET /api/plagiarism` 不依赖模型，优先演示，绝不冷场。
- **周报 / 练习降级**：若 Key 暂不可用，自动返回本地模板（`polished:false`），演示不断流；恢复 Key 后即为混元润色。
- **不要演示**：同步 `POST /api/grade`（易超时），统一用异步轮询。

## 出镜 / 画外音要点

- 不说「秒批」，说「异步轮询、约 1 分钟」——诚实。
- 指标必须是真实跑出的（MAE 9.3 等），不要为了好看改数字。
- 若评委问模型：「我们严格使用腾讯混元 hy3 与 hy-vision-2.0-instruct，符合大赛仅混元要求。」
