# 演示作战手册（决赛现场用）

> 3 分钟演示 + 高频 Q&A + 翻车预案。
> ⚠️ 凡标注 `【待填】` 的数字，**必须**用真实模式（`npm run eval` / `npm run eval:ocr`）跑出来再填，不得估算、不得照抄 MOCK 数据。

---

## 一、上台前 5 分钟检查清单

```bash
node demo/server.mjs                 # 1. 起服务
curl 127.0.0.1:3111/api/health       # 2. 确认 mode=REAL（右上角会显示「混元真实模型」）
curl 127.0.0.1:3111/api/grade/batch  # 3. 预热一次批量，确认不限流
```

- [ ] 右上角显示 **混元真实模型**（若显示 MOCK，说明 `.env.local` 没生效，运行 `npm run key -- <Key>`）
- [ ] 至少准备 **3 张真实手写作业照片**（打印体不算，评委一眼看穿）
- [ ] 录屏备份已存本地（断网时的救命稻草，见第五节）
- [ ] 演示用作业选 **A1 二分查找**（代码题，证据最好看）

---

## 二、3 分钟演示时间轴

| 时间 | 动作 | 说什么（照读即可） |
|---|---|---|
| 0:00–0:20 | 打开页面，直接粘贴一段代码作业 → 点「开始批改」 | 「老师不改一行代码，粘贴就能批。注意每条分数右边都挂着**原文证据**——这是我们的核心：AI 敢给分，就得敢举证。」 |
| 0:20–0:50 | 切到「对话式入口」，连点 3 条指令 | 「但老师真正想要的不是一次批改。他要的是『谁这周掉队了』『给最差的 5 个人出练习』——我们做的是**对话式**教学助手，说人话就出结果。」 |
| 0:50–1:30 | **拍照上传**（预先存好的手写照片，2 张）→ 展示分页转录 → 点「校对后重新批改」 | 「作业是纸质的怎么办？拍照。两页会**合成一份完整答案**再批。这里有个关键设计：识别结果先给老师看，改完再批——AI 不替老师做决定。」 |
| 1:30–2:15 | 点「开始批量批改」，展示进度条与分布 | 「一个班 30 份，并发批改，进度可查，单份失败自动重试不中断。出来的是分数分布，老师一眼看到谁要盯。」 |
| 2:15–2:45 | 指着一个带「待复核」角标的维度 | 「凡是没有原文支撑的评分点，AI 会自己标成**待复核**，老师点一下就能改分写回。人机协同不是口号，是这个角标。」 |
| 2:45–3:00 | 收尾 | 「证据链 + 对话式 + 拍照批改 + 批量，四件事解决的是同一件事：把老师从重复劳动里捞出来。」 |

---

## 三、6 条演示指令（话术固定，别临场发挥）

1. `批改这次提交的作业` → 展示证据链
2. `生成这次作业的学情分析` → 展示维度得分率表
3. `谁这周状态下滑？列个预警名单` → 展示下降幅度
4. `给分数最低的 5 个学生生成个性化练习` → 展示可直接下发
5. `（拍照上传）` → 展示多模态 + 分页合并
6. `（批量批改）` → 展示规模能力

> 每次演示前跑一遍 `npm run smoke`，确认 5/5 PASS 再上台。

---

## 四、决赛 Q&A（口径统一，全队背同一套）

**Q：代码有多少是 AI 写的？**
> 全程用 LearnBuddy 完成，代码里有据可查：`prompts.md` 记着每一次 Prompt 版本迭代的原因与回归结果，`eval/` 下四个自检脚本每次改动都会重跑。AI Coding 的过程本身是可审计的。

**Q：为什么用腾讯混元，不用 GPT/DeepSeek？**
> 两条：一是比赛合规要求；二是我们做的是高校教学场景，数据要能私有化部署，混元 + 本地 SQLite 是一条能真正落地的组合。

**Q：AI 批改错了怎么办？**
> 三层防线。第一层，每一分都必须引用学生原文，找不到证据的维度会被标成 `confidence: low` 并显示「待复核」角标；第二层，老师可以直接改分写回数据库；第三层，拍照作业的转录文本是可编辑的，识别不准先改识别结果再批改。

**Q：手写识别准确率到底多少？**
> 用真实手写照片跑的，字符错误率 XX%、行命中率 XX%、**批改要点召回率 XX%**。【待填：跑 `npm run eval:ocr` 后填】
> 关键说第三个数：CER 稍高不影响判分，决定分数的是那几个关键词有没有保住——我们专门测了这个指标。

**Q：班级数据是真的吗？**
> 不是，是 30 人的虚拟班级，用于验证链路与演示规模。这一点我们会如实说明；真实效果指标以上面的评测报告为准，两者不会混着讲。

**Q：一次能批多少份？多久？**
> 批量任务化：并发可调（默认 3），进度可查，单份失败自动重试 2 次不中断整体。MOCK 模式 30 份 0.54 秒；真实模式【待填：`npm run smoke:batch` 跑出的耗时】。

**Q：如果比赛现场断网 / 模型挂了？**
> 两条降级：产品内置 MOCK 模式，无 Key 也能跑通全链路（今天的自测就是这么跑的）；拍照批改本身是「AI 转录 → 人工校对 → 批改」三步，识别不可用就退化成人工录入，功能不残废。

---

## 五、翻车预案

| 状况 | 现象 | 30 秒内的处置 |
|---|---|---|
| 断网 | 报错「网络不通…请检查是否能访问 api.hunyuan…」 | 立刻切录屏备份继续讲；同时说明「无 Key/断网会降级到 MOCK，这正是我们的降级设计」 |
| 单张照片识别失败 | 该页标红，其余页正常 | 说「单页失败不拖垮整份，这是设计」；在转录框手写补上再批改 |
| 图片太大传不动 | 提示「超过上限，请压缩」 | 手机截图或选「中等画质」；服务端也会自动压缩（页面会显示压缩了百分之多少） |
| 模型返回乱码/非 JSON | 自动重试 3 次 | 等待重试完成；仍失败会说清原因，不甩 raw error |
| 批量卡住 | 进度条不动 | 点「取消任务」重开，结果已批的都落库了不会丢 |
| 接口超时 | 页面提示「超过 XX 秒仍未返回」 | 说明服务端仍在算，可重试或缩小篇幅 |

---

## 六、演示中的三个「别做」

1. **别把 MOCK 数字说成真实效果**——评委问准确率，只报 `eval:ocr` 的真实数据；没跑过就说「还没测，今晚补」。
2. **别临场改 Prompt**——改了就必须走 `prompts.md` 版本记录 + 重跑 eval，现场改会自乱阵脚。
3. **别演示真实学生数据**——虚拟班级就够，涉及隐私反而扣分。
